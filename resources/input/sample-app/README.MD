* try-catch
  * remove 1 exception within multiple inside catch block
  * remove the only exception inside catch block
* Rename methods in the interface
  * rename methods in the implementation class
  * rename the method name wherever it's used
* Replace the factory calls with SpringContext
  * Replace the factory call at the instance level
  * Replace the factory call at the method level
  * Replace the factory call at the constructor level
